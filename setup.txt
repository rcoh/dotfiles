# For xmonad, trayer, dmenu, cabal
sudo apt-get install xmonad trayer suckless-tools cabal-install

# For volume control
cabal install xmonad-extras

# For SSH keyring:
echo 'export SSH_AUTH_SOCK="$GNOME_KEYRING_CONTROL/ssh"' >> ~/.profile

# Modify the session to run start-xmonad
# Change the exec line in /usr/share/xsessions/xmonad.desktop to point to /home/$USER/.xmonad/start-xmonad
# Link spotifyinfo and spotify ctl if desired
ln -s `pwd`/spotifyinfo /usr/bin/spotifyinfo
ln -s `pwd`/spotifyctl /usr/bin/spotifyctl
